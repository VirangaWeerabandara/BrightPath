services:
  backend:
    env_file:
      - ./.env.prod
    ports:
      - "4000:4000"
    restart: always

  frontend:
    build:
      args:
        REACT_APP_API_URL: ${REACT_APP_API_URL}
    ports:
      - "80:80"
    restart: always
